# Единые переводы строк: в репозитории и в рабочей копии всегда LF.
# Проект собирается копированием файлов, поэтому расхождения CRLF/LF
# давали бы шумные диффы между Windows и хостингом.
* text=auto eol=lf
*.svg text eol=lf
*.png binary
*.jpg binary
*.webp binary
